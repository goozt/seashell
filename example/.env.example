# SeaShell API base URL (the Go backend)
# In development, the Next.js rewrite proxies /api/* → this URL
# In production (Vercel), set this to your deployed backend URL
NEXT_PUBLIC_API_URL=http://localhost:8080
